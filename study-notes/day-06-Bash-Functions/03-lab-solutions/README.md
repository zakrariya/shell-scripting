# Lab Solutions

Try every student lab before opening these files.

The Level 03 solutions expect to be run from the `02-student-labs/` directory
because the supplied artifacts are stored there. You can copy the solution
scripts into that directory before testing them.

Validate a script:

```bash
bash -n script_name.sh
```

Run it:

```bash
bash script_name.sh
```

